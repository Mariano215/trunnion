{
  "$comment": "Primitive 12 — declared authority, tracked in version control. Trunnion diffs this against what is actually loaded and reports drift. Do not move these values into an untracked local settings file; that is the exact failure the rubric's worked example documents.",
  "permissions": {
    "allow": [
      "Read(**)",
      "Bash(cargo build:*)",
      "Bash(cargo test:*)",
      "Bash(cargo clippy:*)",
      "Bash(cargo fmt:*)",
      "Bash(git status:*)",
      "Bash(git diff:*)",
      "Bash(git log:*)",
      "Write(**)",
      "Edit(**)",
      "Bash(git commit:*)",
      "Bash(docker:*)"
    ],
    "ask": [
    ],
    "deny": [
      "Read(./.env)",
      "Read(./.env.*)",
      "Read(**/*.pem)",
      "Read(**/id_rsa*)",
      "Read(./secrets/**)",
      "Bash(git push:*)",
      "Bash(curl:*)",
      "Bash(wget:*)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PROJECT_DIR}/.claude/hooks/permission-mode.sh"
          }
        ]
      }
    ]
  }
}
